// Android shouldn't use a navigation spacer since it's
// not using the Blur View feature. To compensate for this,
// we're just exporting a null component so nothing will render
export default () => null;
